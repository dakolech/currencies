import styled from 'styled-components';
import { black } from '../../config/styles/colors';

export const CurrencyContainerComponent: any = styled.div`
  width: 300px;
  padding: 10px;
  margin: 10px;
  border: 1px solid ${black};
  position: relative;
`;
